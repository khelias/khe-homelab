# Dashboard provider — intentionally empty until v1 has dashboards to
# load. Enabling a file provider with a path that doesn't exist makes
# Grafana log a "failed to search for dashboards" error every 30s,
# which we don't want polluting Loki. Add a provider here together
# with the first dashboard JSON.
apiVersion: 1
providers: []
