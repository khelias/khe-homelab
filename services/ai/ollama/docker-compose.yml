services:
  ollama:
    image: ollama/ollama:0.32.13@sha256:77f1a2a54460f0380f2611e1464233d9b82cb6e58afc8f60abec0061049d2d82
    container_name: ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    environment:
      - TZ=Europe/Tallinn
      - OLLAMA_NUM_THREAD=8        # use all vCPUs explicitly
      - OLLAMA_KEEP_ALIVE=-1       # keep model loaded in RAM indefinitely
      - OLLAMA_FLASH_ATTENTION=1   # memory-efficient attention (Ollama v0.13.5+)
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - ai-internal
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 10G   # qwen2.5:7b ~5-6GB + overhead
          cpus: '6'     # leave 2 vCPUs for other services during inference
        # GPU passthrough (when available — uncomment and remove limits above):
        # reservations:
        #   devices:
        #     - capabilities: [gpu]

volumes:
  ollama_data:

networks:
  ai-internal:
    external: true
