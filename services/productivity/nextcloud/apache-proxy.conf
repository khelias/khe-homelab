# Tell Apache the connection is HTTPS when behind a reverse proxy (CF Tunnel).
# Without this, .htaccess RewriteRule redirects (e.g. .well-known/caldav)
# generate http:// Location headers instead of https://.
<IfModule mod_setenvif.c>
    SetEnvIf X-Forwarded-Proto "https" HTTPS=on
</IfModule>
